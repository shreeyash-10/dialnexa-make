{
  "url": "/v1/agents",
  "method": "GET",
  "response": {
    "iterate": "{{ifempty(body.data.agents, body.agents)}}",
    "output": {
      "label": "{{ifempty(item.current_version.title, item.id)}}",
      "value": "{{item.id}}"
    },
    "limit": 500
  }
}
